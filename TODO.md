show job levels and user ID in GUI. 
Have external file where we can list users that gets embedded to program on build time. then parse from that file users to a map and if not found just show ID else show username.
so the file has username -> id
add to gui logs

reset_skill_que -> call this everytime mode changes

whole dps mode needs redoing
raid mode somehow working with gui?

ts73 bot?
